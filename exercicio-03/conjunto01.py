# Implementando união e intersecção de conjuntos:
conjunto1 = set({1, 2, 3, 4, 5, 6, 7})
conjunto2 = set({3, 4, 5, 6, 7, 8, 9})

print(f"União: {conjunto1.union(conjunto2)}")
print(f"Intersecção:  {conjunto1.intersection(conjunto2)}")
